import Auth0WebAuth, {
  TYPE as AUTH0_WEB_AUTH
} from './auth0WebAuth';
import MachineAuth, {
  TYPE as MACHINE_AUTH
} from './machineAuth';

const TYPES = {
  AUTH0_WEB_AUTH,
  MACHINE_AUTH
};

export {
  Auth0WebAuth,
  MachineAuth,
  TYPES
};
